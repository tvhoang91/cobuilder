import { AiModel, Block } from '@/schema'

export function generateCodeWireframe(block: Block, textWireframe: string, prompts: string[], aiModel: AiModel) {
  return ''
}

export function iterateCodeWireframe(block: Block, prompts: string[]) {
  return ''
}
